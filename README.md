# 动态数码管显示

四位数码管动态扫描显示 0 到 9 的循环计数结果。

- 顶层：`dymc_seg`
- 时钟：33.33 MHz
- 目标器件：XC7Z020-CLG400-1

`en` 是原始顶层保留端口，当前 RTL 未使用该信号；该事实已由综合检查确认。

在 Vivado 中执行 `source create_project.tcl` 即可重建工程。约束文件来自原工程，下载前请确认与实际板卡一致。
